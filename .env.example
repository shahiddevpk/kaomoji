# Google Search Console HTML-tag verification code (meta content value only)
# Leave empty until you have the real code from GSC → Settings → Ownership verification → HTML tag
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
